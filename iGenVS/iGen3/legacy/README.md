# Legacy Scripts

The original duplicated sampling scripts were moved here for reference only.

Use the maintained CLI instead:

```bash
igen3 generate --model base-isomeric --mode de-novo --count 1000 --output outputs/base_iso.smi
```

