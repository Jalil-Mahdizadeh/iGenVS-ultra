[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "igenvs-ultra-pipeline"
version = "0.2.0"
description = "User-facing molecular docking, ultra-scale screening, and target-specific iGen3 RL"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
dependencies = []

[project.scripts]
igenvs-ultra = "igenvs_ultra.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
